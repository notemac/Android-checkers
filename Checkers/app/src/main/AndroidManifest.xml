<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.bstu.checkers">

    <permission android:name="ru.bstu.checkers.provider.READ_WRITE"/>
    
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        >

        <provider
            android:authorities="ru.bstu.checkers.provider"
            android:name=".MyContentProvider"
            android:exported="true"
            android:permission="ru.bstu.checkers.provider.READ_WRITE"
            />

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PreferencesActivity"
            android:label="@string/label_PreferencesActivity"
            android:screenOrientation="portrait" />

        <activity android:name=".AnimatedActivity" />

        <activity android:name=".HandmadeActivity" />

        <activity
            android:name=".NewSavedGameActivity"
            android:label="@string/label_NewSavedGameActivity"
            android:screenOrientation="portrait" />

        <activity android:name=".LoadGameActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/label_LoadGameActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>